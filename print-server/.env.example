# Modo: mock | network (fallback servidor — preferir /configuracion/impresora)
PRINT_MODE=mock

# Puerto del servidor local
PORT=3100

# Impresora principal única (fallback — preferir configuración en la app)
PRINTER_IP=
PRINTER_PORT=9100
