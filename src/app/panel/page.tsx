import { PanelClient } from "@/components/panel/PanelClient";

export const metadata = {
  title: "Panel cocina/barra · Comandero",
};

export default function PanelPage() {
  return <PanelClient />;
}
